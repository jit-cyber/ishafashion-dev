# .htaccess

# Enable URL rewriting
RewriteEngine On

# Redirect root URL to the home page
RewriteRule ^$ index.php [L]

# Handle the routing of requests to the appropriate files
RewriteRule ^([a-zA-Z0-9_-]+)/?$ index.php [L]